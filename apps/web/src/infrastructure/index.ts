/**
 * Browser-specific adapters live here.
 * Persistence, wall-clock access and worker integration must stay outside Domain/Engine.
 */
export const infrastructureLayer = "web-infrastructure" as const;
